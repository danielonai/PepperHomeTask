import { ContactState } from "../types/contactsState";
import { UserState } from "../types/userState";
  
  export const contactsInitialState: ContactState = {
    contactsList: [],
  }
  
  export const userInitialState: UserState = {
    user: undefined,
  }
  